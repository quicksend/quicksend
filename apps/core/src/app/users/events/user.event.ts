export enum UserEvent {
  CREATED = "auth.user.created",
  DELETED = "auth.user.deleted",
  EMAIL_CONFIRMED = "auth.user.email-confirmed",
  PASSWORD_CHANGED = "auth.user.password-changed",
  UPDATED = "auth.user.updated"
}
